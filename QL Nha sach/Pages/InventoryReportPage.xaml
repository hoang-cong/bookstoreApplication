<Page x:Class="QL_Nha_sach.Pages.InventoryReportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QL_Nha_sach.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="InventoryReportPage">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Line items -->
            <RowDefinition Height="Auto"/>
            <!-- Totals + buttons -->
        </Grid.RowDefinitions>

        <Button Content="✖" HorizontalAlignment="Right" VerticalAlignment="Top" Click="ExitButton_Click"/>

        <StackPanel>
            <TextBlock Text="Inventory Report"
                       FontSize="20"
                       FontWeight="Bold"
                       Margin="0,0,40,20"/>

            <!-- DatePicker for snapshot date -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Select Date:" VerticalAlignment="Center"/>
                <DatePicker SelectedDate="{Binding SelectedDate}" Width="150" Margin="10,0,20,0"/>
                <Button Content="Load Report" Command="{Binding LoadInventoryCommand}" Width="120"/>
            </StackPanel>

            <!-- DataGrid showing stock -->
            <DataGrid ItemsSource="{Binding LowStockBooks}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      Margin="0,0,0,20">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Title" Binding="{Binding Title}" />
                    <DataGridTextColumn Header="ISBN" Binding="{Binding ISBN}" />
                    <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" />
                    <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat={}{0:C}}" />
                    <DataGridTextColumn Header="Status" Binding="{Binding BookStatus.StatusName}" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</Page>
