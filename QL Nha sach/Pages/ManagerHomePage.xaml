<Page x:Class="QL_Nha_sach.Pages.ManagerHomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QL_Nha_sach.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ManagerHomePage">

    <Grid>
        <StackPanel>
            <Button Content="Manage Books" Command="{Binding ManageBooksCommand}"/>
            <Button Content="Transaction List" Command="{Binding OpenTransactionListCommand}"/>
            <Button Content="Logout" FontWeight="Bold" Click="LogoutButton_Click"/>
        </StackPanel>

        <StackPanel x:Name="SessionContext" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Logged in as:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding FullName}" VerticalAlignment="Center" FontWeight="Bold"/>
            <TextBlock Text=" (" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding RoleName}" VerticalAlignment="Center"/>
            <TextBlock Text=")" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>
