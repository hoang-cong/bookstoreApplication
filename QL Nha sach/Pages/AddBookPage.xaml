<Page x:Class="QL_Nha_sach.Pages.AddBookPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QL_Nha_sach.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddBookPage">

    <Grid Background="White" Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <!-- Main form -->
            <RowDefinition Height="Auto"/>
            <!-- Save button -->
        </Grid.RowDefinitions>

        <!-- Exit button -->
        <Button Content="✖"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Width="30" Height="30"
                Background="Transparent"
                BorderThickness="0"
                Foreground="Red"
                FontWeight="Bold"
                Click="ExitButton_Click"/>

        <!-- Main form: two-column layout -->
        <Grid Grid.Row="0" Margin="0,40,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <!-- Left: text inputs -->
                <ColumnDefinition Width="40"/>
                <!-- Spacer -->
                <ColumnDefinition Width="*"/>
                <!-- Right: dropdowns -->
            </Grid.ColumnDefinitions>

            <!-- Left side: typing boxes -->
            <StackPanel Grid.Column="0">
                <TextBlock Text="Book Title" Margin="0,0,0,5" FontWeight="SemiBold"/>
                <TextBox Text="{Binding NewBook.Title}" Padding="5" Height="30"/>

                <TextBlock Text="ISBN Number" Margin="0,10,0,5" FontWeight="SemiBold"/>
                <TextBox Text="{Binding NewBook.ISBN}" Padding="5" Height="30"/>

                <TextBlock Text="Price ($)" Margin="0,10,0,5" FontWeight="SemiBold"/>
                <TextBox Text="{Binding NewBook.Price}" Padding="5" Height="30"/>

                <TextBlock Text="Initial Stock" Margin="0,10,0,5" FontWeight="SemiBold"/>
                <TextBox Text="{Binding NewBook.Stock}" Padding="5" Height="30"/>
            </StackPanel>

            <!-- Right side: dropdowns with manage buttons -->
            <StackPanel Grid.Column="2">
                <TextBlock Text="Author" Margin="0,0,0,5" FontWeight="SemiBold"/>
                <DockPanel>
                    <ComboBox ItemsSource="{Binding Authors}" Height="30">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding AuthorName}" IsChecked="{Binding IsSelected, Mode=TwoWay}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <Button Content="Manage" Command="{Binding ManageAuthorCommand}" DockPanel.Dock="Right" Margin="5,0,0,0" Padding="10,0"/>
                </DockPanel>

                <TextBlock Text="Genre" Margin="0,10,0,5" FontWeight="SemiBold"/>
                <DockPanel>
                    <ComboBox ItemsSource="{Binding Genres}" Height="30">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding GenreName}" IsChecked="{Binding IsSelected, Mode=TwoWay}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <Button Content="Manage" Command="{Binding ManageGenreCommand}" DockPanel.Dock="Right" Margin="5,0,0,0" Padding="10,0"/>
                </DockPanel>
                
                <TextBlock Text="Publisher" Margin="0,10,0,5" FontWeight="SemiBold"/>
                <DockPanel>
                    <Button Content="Manage" Command="{Binding ManagePublisherCommand}" DockPanel.Dock="Right" Margin="5,0,0,0" Padding="10,0"/>
                    <ComboBox ItemsSource="{Binding Publishers}" DisplayMemberPath="PublisherName" SelectedValuePath="PublisherId" SelectedValue="{Binding NewBook.PublisherId}" Height="30"/>
                </DockPanel>

                <TextBlock Text="Book Status" Margin="0,10,0,5" FontWeight="SemiBold"/>
                <DockPanel>
                    <ComboBox ItemsSource="{Binding BookStatuses}" DisplayMemberPath="StatusName" SelectedValuePath="BookStatusId" SelectedValue="{Binding NewBook.BookStatusId}" Height="30"/>
                </DockPanel>
            </StackPanel>
        </Grid>

        <!-- Save button bottom-left -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,10,0,0">
            <Button Command="{Binding SaveBookCommand}" 
                    Content="Save Book to System" 
                    Padding="15,5"
                    Background="#27ae60" 
                    Foreground="White" 
                    FontWeight="Bold"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Page>
