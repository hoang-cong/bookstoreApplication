<Page x:Class="QL_Nha_sach.Pages.AddBookPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QL_Nha_sach.Pages"
      xmlns:controls="clr-namespace:QL_Nha_sach.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddBookPage">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Controls/SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

	<Grid Background="#F3F4F6">

			<Border Background="White" CornerRadius="12" Margin="40" VerticalAlignment="Center" HorizontalAlignment="Center" Width="850" Padding="32">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="*"/>
						<RowDefinition Height="Auto"/>
					</Grid.RowDefinitions>

					<!-- 1. Header Section -->
					<Grid Grid.Row="0" Margin="0,0,0,24">
						<TextBlock Text="{DynamicResource Str_AddNewBook}" FontSize="22" FontWeight="Bold" Foreground="#111827"/>
						<Button Content="✖" HorizontalAlignment="Right" VerticalAlignment="Top" Style="{StaticResource ExitButton}" Click="ExitButton_Click"/>
					</Grid>

					<!-- 2. Main Form Layout -->
					<Grid Grid.Row="1">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="48"/>
							<ColumnDefinition Width="*"/>
						</Grid.ColumnDefinitions>

						<!-- Left Column: Primary Details -->
						<StackPanel Grid.Column="0">
							<TextBlock Text="{DynamicResource Str_Title}" Foreground="#4B5563" FontSize="14" FontWeight="Medium" Margin="0,0,0,6"/>
							<TextBox Text="{Binding NewBook.Title}" Height="36" VerticalContentAlignment="Center" Padding="8,0" Margin="0,0,0,16"/>

							<TextBlock Text="ISBN" Foreground="#4B5563" FontSize="14" FontWeight="Medium" Margin="0,0,0,6"/>
							<Grid Margin="0,0,0,16">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*"/>
									<ColumnDefinition Width="Auto"/>
								</Grid.ColumnDefinitions>
								<TextBox Text="{Binding NewBook.ISBN}" Height="36" VerticalContentAlignment="Center" Padding="8,0"/>
								<Button Grid.Column="1" Content="Autofill" Command="{Binding AutofillBookCommand}" Margin="8,0,0,0" Padding="12,0" Background="#F3F4F6" BorderBrush="#E5E7EB"/>
							</Grid>

							<TextBlock Text="{DynamicResource Str_Price}" Foreground="#4B5563" FontSize="14" FontWeight="Medium" Margin="0,0,0,6"/>
							<TextBox Text="{Binding NewBook.Price}" Height="36" VerticalContentAlignment="Center" Padding="8,0" Margin="0,0,0,16"/>

							<TextBlock Text="{DynamicResource Str_Stock}" Foreground="#4B5563" FontSize="14" FontWeight="Medium" Margin="0,0,0,6"/>
							<TextBox Text="{Binding NewBook.Stock}" Height="36" VerticalContentAlignment="Center" Padding="8,0"/>
						</StackPanel>

						<!-- Right Column: Categorization -->
						<StackPanel Grid.Column="2">
							<TextBlock Text="{DynamicResource Str_Author}" Foreground="#4B5563" FontSize="14" FontWeight="Medium" Margin="0,0,0,6"/>
							<DockPanel Margin="0,0,0,16">
								<Button Content="{DynamicResource Str_Manage}" Command="{Binding ManageAuthorCommand}" DockPanel.Dock="Right" Margin="8,0,0,0" Background="Transparent" Foreground="#2563EB" BorderThickness="0" FontWeight="SemiBold"/>
								<ComboBox ItemsSource="{Binding Authors}" Height="36" VerticalContentAlignment="Center">
									<ComboBox.ItemTemplate>
										<DataTemplate>
											<ToggleButton IsChecked="{Binding IsSelected, Mode=TwoWay}" HorizontalContentAlignment="Left" Padding="5">
												<StackPanel Orientation="Horizontal">
													<TextBlock Text="{Binding AuthorName}" />
												</StackPanel>
											</ToggleButton>
										</DataTemplate>
									</ComboBox.ItemTemplate>

									<ComboBox.ItemContainerStyle>
										<Style TargetType="ComboBoxItem">
											<Setter Property="HorizontalContentAlignment" Value="Stretch"/>
										</Style>
									</ComboBox.ItemContainerStyle>
								</ComboBox>
							</DockPanel>

							<TextBlock Text="{DynamicResource Str_Genre}" Foreground="#4B5563" FontSize="14" FontWeight="Medium" Margin="0,0,0,6"/>
							<DockPanel Margin="0,0,0,16">
								<Button Content="{DynamicResource Str_Manage}" Command="{Binding ManageGenreCommand}" DockPanel.Dock="Right" Margin="8,0,0,0" Background="Transparent" Foreground="#2563EB" BorderThickness="0" FontWeight="SemiBold"/>
								<ComboBox ItemsSource="{Binding Genres}" Height="36" VerticalContentAlignment="Center">
									<ComboBox.ItemTemplate>
										<DataTemplate>
											<ToggleButton IsChecked="{Binding IsSelected, Mode=TwoWay}" HorizontalContentAlignment="Left" Padding="5">
												<StackPanel Orientation="Horizontal">
													<TextBlock Text="{Binding GenreName}" />
												</StackPanel>
											</ToggleButton>
										</DataTemplate>
									</ComboBox.ItemTemplate>

									<ComboBox.ItemContainerStyle>
										<Style TargetType="ComboBoxItem">
											<Setter Property="HorizontalContentAlignment" Value="Stretch"/>
										</Style>
									</ComboBox.ItemContainerStyle>
								</ComboBox>
							</DockPanel>

							<TextBlock Text="{DynamicResource Str_Publisher}" Foreground="#4B5563" FontSize="14" FontWeight="Medium" Margin="0,0,0,6"/>
							<DockPanel Margin="0,0,0,16">
								<Button Content="{DynamicResource Str_Manage}" Command="{Binding ManagePublisherCommand}" DockPanel.Dock="Right" Margin="8,0,0,0" Background="Transparent" Foreground="#2563EB" BorderThickness="0" FontWeight="SemiBold"/>
								<ComboBox ItemsSource="{Binding Publishers}" DisplayMemberPath="PublisherName" SelectedValuePath="PublisherId" SelectedValue="{Binding NewBook.PublisherId}" Height="36" VerticalContentAlignment="Center"/>
							</DockPanel>

							<TextBlock Text="{DynamicResource Str_BookStatus}" Foreground="#4B5563" FontSize="14" FontWeight="Medium" Margin="0,0,0,6"/>
							<ComboBox ItemsSource="{Binding BookStatuses}" DisplayMemberPath="StatusName" SelectedValuePath="BookStatusId" SelectedValue="{Binding NewBook.BookStatusId}" Height="36" VerticalContentAlignment="Center"/>
						</StackPanel>
					</Grid>

					<!-- 3. Footer Action Section -->
					<StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,32,0,0">
						<Button Content="{DynamicResource Str_Save}" Command="{Binding SaveBookCommand}" Style="{StaticResource SaveButton}" Padding="24,10"/>
					</StackPanel>
				</Grid>
			</Border>

		<controls:MessageBanner HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0" Panel.ZIndex="100"/>
	</Grid>
</Page>
