<Page x:Class="QL_Nha_sach.Pages.SalesReportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QL_Nha_sach.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SalesReportPage">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <!-- Main form -->
            <RowDefinition Height="Auto"/>
            <!-- Save button -->
        </Grid.RowDefinitions>

        <!-- Exit button -->
        <Button Content="✖"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Click="ExitButton_Click"/>

        <StackPanel>
            <TextBlock Text="Sales Report by Genre"
                       FontSize="20"
                       FontWeight="Bold"
                       Margin="0,0,0,20"/>

            <!-- DatePicker for month/year selection -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Select Month:" VerticalAlignment="Center"/>
                <DatePicker SelectedDate="{Binding SelectedDate}" Width="150" Margin="10,0,20,0"/>
                <Button Content="Load Report" Command="{Binding LoadReportCommand}" Width="100"/>
            </StackPanel>

            <!-- DataGrid -->
            <DataGrid ItemsSource="{Binding GenreSales}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      Margin="0,0,0,20">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Genre" Binding="{Binding Genre}" />
                    <DataGridTextColumn Header="Quantity Sold" Binding="{Binding Quantity}" />
                    <DataGridTextColumn Header="Revenue" Binding="{Binding Revenue, StringFormat={}{0:C}}" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <TextBlock Text="{Binding TotalQuantity, StringFormat=Total Quantity: {0}}"
                       FontWeight="Bold"
                       HorizontalAlignment="Left"
                       Margin="0,0,0,5"/>
            <TextBlock Text="{Binding TotalRevenue, StringFormat=Total Revenue: {0:C}}"
                       FontWeight="Bold"
                       HorizontalAlignment="Left"/>
        </StackPanel>
    </Grid>
</Page>
