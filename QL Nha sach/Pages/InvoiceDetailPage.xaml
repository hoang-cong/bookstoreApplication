<Page x:Class="QL_Nha_sach.Pages.InvoiceDetailPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QL_Nha_sach.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="InvoiceDetailPage">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Details grid -->
            <RowDefinition Height="Auto"/>
            <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- Header info -->
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,10">
            <TextBlock Text="Invoice Detail" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Invoice ID: " FontWeight="Bold"/>
                <TextBlock Text="{Binding InvoiceId}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Date: " FontWeight="Bold"/>
                <TextBlock Text="{Binding Date}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Staff: " FontWeight="Bold"/>
                <TextBlock Text="{Binding StaffName}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Status: " FontWeight="Bold"/>
                <TextBlock Text="{Binding IsVoided}"/>
            </StackPanel>
        </StackPanel>

        <!-- Line items -->
        <DataGrid Grid.Row="1"
              ItemsSource="{Binding Details}"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              Margin="0,10,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Book" Binding="{Binding Book.Title}" />
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" />
                <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice}" />
                <DataGridTextColumn Header="Discount" Binding="{Binding Discount}" />
                <DataGridTextColumn Header="Subtotal" Binding="{Binding SubTotal}" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <Button Grid.Row="2" Content="Back" HorizontalAlignment="Right" Width="80" Click="ExitButton_Click"/>
        <Button Grid.Row="2" Content="Void Invoice" HorizontalAlignment="Left" Width="80" Command="{Binding VoidInvoiceCommand}"/>
    </Grid>

</Page>
