<Window x:Class="QL_Nha_sach.Pages.LookupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QL_Nha_sach.Pages"
        mc:Ignorable="d"
        Title="{Binding Title}" 
        Height="450" Width="350" 
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="{Binding Title, StringFormat='Manage {0}s'}" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <TextBox Grid.Row="1" 
                 Text="{Binding NewName, UpdateSourceTrigger=PropertyChanged}" 
                 Tag="Enter name here..."
                 Padding="5" Margin="0,0,0,10"/>

        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" 
                    Content="Add New" 
                    Command="{Binding AddCommand}" 
                    Margin="0,0,5,0" Padding="10,5"
                    Background="#E1F5FE"/>
            <Button Grid.Column="1" 
                    Content="Delete Selected" 
                    Command="{Binding DeleteCommand}" 
                    Margin="5,0,0,0" Padding="10,5"
                    Background="#FFEBEE" Foreground="#C62828"/>
        </Grid>

        <ListBox Grid.Row="3" 
                 ItemsSource="{Binding Items}" 
                 SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                 DisplayMemberPath="Name" 
                 Margin="0,0,0,10">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="5"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <Button Grid.Row="4" 
                Content="Done" 
                Click="Close_Click" 
                HorizontalAlignment="Right"
                Width="100" Padding="10,5" IsDefault="True"/>
    </Grid>
</Window>
