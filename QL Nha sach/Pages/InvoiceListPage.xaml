<Page x:Class="QL_Nha_sach.Pages.InvoiceListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QL_Nha_sach.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="InvoiceListPage">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- DataGrid -->
            <RowDefinition Height="Auto"/>
            <!-- Footer buttons -->
        </Grid.RowDefinitions>

        <!-- Exit button -->
        <Button Content="✖"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Width="30" Height="30"
                Background="Transparent"
                BorderThickness="0"
                Foreground="Red"
                FontWeight="Bold"
                Click="ExitButton_Click"/>

        <!-- DataGrid -->
        <DataGrid x:Name="InvoiceGrid"
                  Grid.Row="1"
                  ItemsSource="{Binding Invoices}"
                  SelectedItem="{Binding SelectedInvoice}"
                  AutoGenerateColumns="False"
                  Margin="0,40,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding InvoiceId}" />
                <DataGridTextColumn Header="Date" Binding="{Binding InvoiceDate}" />
                <DataGridTextColumn Header="Staff" Binding="{Binding UserId}" />
                <!-- Show Voided status -->
                <DataGridTextColumn Header="Status" 
                                    Binding="{Binding IsVoided}" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,10,0,0">
            <Button Content="View Details" Command="{Binding ViewInvoiceDetailCommand}"/>
        </StackPanel>
    </Grid>
</Page>
