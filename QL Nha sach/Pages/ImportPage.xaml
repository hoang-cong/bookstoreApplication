<Page x:Class="QL_Nha_sach.Pages.ImportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QL_Nha_sach.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ImportPage">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Line items -->
            <RowDefinition Height="Auto"/>
            <!-- Totals + buttons -->
        </Grid.RowDefinitions>

        <Button Content="✖"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Width="30" Height="30"
                Background="Transparent"
                BorderThickness="0"
                Foreground="Red"
                FontWeight="Bold"
                Click="ExitButton_Click"/>

        <!-- Header info -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Scan ISBN:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Width="200" Height="30" Text="{Binding ScannedISBN, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="Import Date:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding Date}" VerticalAlignment="Center"/>
            <TextBlock Text="   Staff:" VerticalAlignment="Center" Margin="20,0,10,0"/>
            <TextBlock Text="{Binding StaffName}" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Line items -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Details}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Margin="0,0,0,10"
                  SelectedItem="{Binding SelectedDetail, Mode=TwoWay}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ISBN" Binding="{Binding Book.ISBN}" Width="100"/>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="200"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="100"/>
                <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, StringFormat={}{0:C}}" Width="120"/>
                <DataGridTextColumn Header="SubTotal" Binding="{Binding SubTotal, StringFormat={}{0:C}}" Width="120" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Totals + buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock Text="Total:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding Total, StringFormat={}{0:C}}" VerticalAlignment="Center" Margin="0,0,20,0"/>
            <Button Content="Save Import" Command="{Binding SaveFormCommand}" Background="#27ae60" Foreground="White" FontWeight="Bold"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,10,0,0">
            <Button Content="Add Book" Command="{Binding AddScannedBookCommand}" Margin="10,0,0,0"/>
            <Button Content="Remove Item" Command="{Binding RemoveItemCommand}" CommandParameter="{Binding SelectedDetail}" Margin="10, 0, 0, 0"/>
        </StackPanel>
    </Grid>
</Page>
